# Everything needed to run the scraper, classifier, scoring, database and
# alerts. PDF extraction, OCR, spaCy, Playwright and the ML layer all degrade
# gracefully when absent, so this is enough for a first deployment.
requests>=2.31,<3
pandas>=2.1,<3
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
PyYAML>=6.0,<7
pytest>=8.0,<9
